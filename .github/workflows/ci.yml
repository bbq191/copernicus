name: CI

on:
  push:
    branches: [master]
  pull_request:

jobs:
  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/pyproject.toml
      # 测试全部使用 mock，不需要 GPU；先装 CPU 版 torch，避免下载数 GB 的 CUDA 轮子
      - name: Install CPU-only torch
        run: pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
      - name: Install package
        run: pip install -e ".[dev]"
      - name: Lint
        run: ruff check src tests
      - name: Test
        run: pytest -q

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - name: Type check
        run: npx tsc -b
      - name: Lint
        run: npm run lint
      - name: Test
        run: npm test
      - name: Build
        run: npm run build

  deploy-scripts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # 安装/卸载脚本只能在真机上完整验证；CI 至少保证语法与静态检查通过
      - name: ShellCheck
        run: shellcheck -x deploy/install.sh deploy/uninstall.sh deploy/lib.sh
      - name: Dry run
        run: sudo deploy/install.sh --dry-run --skip-frontend --no-nginx --python "$(command -v python3)"
